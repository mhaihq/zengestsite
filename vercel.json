{
  "buildCommand": "echo 'Serving committed pre-built dist/ (run pnpm build:prerender locally before deploying)'",
  "outputDirectory": "dist",
  "routes": [
    { "handle": "filesystem" },
    { "src": "/[^.]+", "dest": "/index.html", "status": 200 }
  ]
}
